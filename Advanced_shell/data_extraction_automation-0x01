#!/bin/bash
if [ ! -f data.json ]; then
  echo "data.json not found" >&2
  exit 1
fi

name=$(jq -r '.name' data.json | sed 's/.*/\u&/')
type=$(jq -r '.types[0].type.name' data.json | sed 's/.*/\u&/')
height=$(jq -r '.height' data.json)
weight=$(jq -r '.weight' data.json)

formatted_height=$(awk -v h="$height" 'BEGIN{printf "%.1f", h/10}')
formatted_weight=$(awk -v w="$weight" 'BEGIN{printf "%.1f", w/10}')

formatted_height=${formatted_height%\.0}
formatted_weight=${formatted_weight%\.0}

# "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."

echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
